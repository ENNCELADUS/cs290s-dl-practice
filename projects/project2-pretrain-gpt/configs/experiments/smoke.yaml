seed: 42
output_dir: outputs/smoke
model_config: configs/models/tiny.json
dataset_name: roneneldan/TinyStories
dataset_config_name: null
dataset_local_files_only: false
tokenizer_name_or_path: roneneldan/TinyStories-33M
tokenizer_local_files_only: false
max_train_samples: 128
max_validation_samples: 64
num_preprocessing_workers: 1
dataloader_num_workers: 0
per_device_train_batch_size: 2
per_device_eval_batch_size: 2
gradient_accumulation_steps: 1
learning_rate: 0.0008
weight_decay: 0.1
warmup_ratio: 0.02
max_train_steps: 20
eval_every_steps: 10
save_every_steps: 20
logging_every_steps: 1
max_eval_batches: 4
max_grad_norm: 1.0
sample_prompt: "Once upon a time"
sample_num_return_sequences: 1
sample_max_new_tokens: 40
sample_temperature: 0.9
sample_top_k: 50
sample_top_p: 0.95
